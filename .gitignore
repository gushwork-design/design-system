
__pycache__/

# Private to this machine — the repo is public. See the note at the top of BACKLOG.md.
BACKLOG.md
preview/board.html

# Rendered output of templates/*/render.sh — regenerate with ./render.sh
templates/**/*.pdf

# Slide-deck template build output and deps — regenerate with ./render.sh and
# `npm install && node build-pptx.mjs`
templates/**/node_modules/
templates/**/out/
templates/**/package-lock.json
.vercel

# Rendered ad-page video. 7MB, and a fresh copy lands on every re-render — the source
# is the HyperFrames project at videos/ai-crm-flow/, and publish-sheets.sh ships the
# working tree, so deploys do not need it in git.
web/internal/staging/**/ai-crm-flow.mp4
